# Vercel/production frontend env.
# Do not include the trailing /api; the frontend adds /api automatically.
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# Server-side Next API route fallback.
BACKEND_BASE_URL=https://api.example.com

